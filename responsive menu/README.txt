A Pen created at CodePen.io. You can find this one at https://codepen.io/micahgodbolt/pen/mnLiF.

 :target pseudo selectors ftw! The hardest trick was getting the CSS animations working properly. Turns out you can't animate a change from 0px to auto, so instead of changing the height of the UL container, I had to change the height of each li to a set height. 

Next challenge is tertiary navigation.